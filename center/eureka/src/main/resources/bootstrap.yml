spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8887
      name: eureka-server-config
      label: master
#      profile: dev
      enabled: true
      fail-fast: false
      retry:
        max-attempts: 6
    bus:
      trace:
        enabled: true