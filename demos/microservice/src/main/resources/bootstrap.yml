spring:
  application:
    name: microservice
  cloud:
    config:
      name: microservice-config
      label: master
#      profile: dev
      enabled: true
      fail-fast: false
      retry:
        max-attempts: 6
    bus:
      trace:
        enabled: true